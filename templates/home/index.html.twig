<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Home</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background: url('photo2.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #fff;
        }
        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
        }
        .container {
            text-align: center;
            position: relative;
            z-index: 1;
            padding: 20px; /* Ajouté pour espacer le contenu de la bordure */
        }
        .title-top {
            margin-top: 20px;
            font-size: 24px;
        }
        .title-center {
            margin-top: 150px; /* Ajustez cette valeur selon le besoin */
            margin-bottom: 20px;
            font-size: 48px;
            font-weight: bold;
        }
        .subtitle-center {
            margin-top: 20px;
            margin-bottom: 20px;
            font-size: 24px;
        }
        .btn {
            padding: 10px 20px;
            font-size: 18px;
            color: #fff;
            background-color: #007BFF;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
        }
        .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="overlay"></div>
    <div class="container">
        <div class="title-top">Home</div>
        <div class="title-center">WELCOME TO YOUR ENVIRONMENT</div>
        <div class="subtitle-center">Your easiest way for better note tracking</div>
        <a href="{{ path('app_etudiant_index') }}" class="btn">Get Started</a>
    </div>
</body>
</html>
